package com.example.beadando;

import com.example.beadando.Kapcsolat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KapcsolatRepository extends JpaRepository<Kapcsolat, Long> {
}

